<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.uoquo.platform.system.mapper.AppInheritMapper">

  <resultMap id="BaseResultMap" type="com.uoquo.platform.system.model.pojo.AppInherit">
    <id column="id" jdbcType="VARCHAR" property="id" />
    <result column="app_id" jdbcType="VARCHAR" property="appId" />
    <result column="parent_id" jdbcType="VARCHAR" property="parentId" />
    <result column="create_user" jdbcType="VARCHAR" property="createUser" />
    <result column="create_time" jdbcType="TIMESTAMP" property="createTime" />
  </resultMap>

  <sql id="Base_Column_List">
    id, app_id, parent_id, create_user, create_time
  </sql>

  <insert id="insert" parameterType="com.uoquo.platform.system.model.pojo.AppInherit">
    INSERT INTO bko_app_inherit (id, app_id, parent_id,
      create_user, create_time)
    VALUES (#{id,jdbcType=VARCHAR}, #{appId,jdbcType=VARCHAR}, #{parentId,jdbcType=VARCHAR},
      #{createUser,jdbcType=VARCHAR}, #{createTime,jdbcType=TIMESTAMP})
  </insert>

  <insert id="batchInsert" parameterType="java.util.List">
    INSERT INTO bko_app_inherit (id, app_id, parent_id,
      create_user, create_time) VALUES
    <foreach collection="list" item="item" separator=",">
      (#{item.id,jdbcType=VARCHAR}, #{item.appId,jdbcType=VARCHAR}, #{item.parentId,jdbcType=VARCHAR},
        #{item.createUser,jdbcType=VARCHAR}, #{item.createTime,jdbcType=TIMESTAMP})
    </foreach>
  </insert>

  <delete id="deleteByPrimaryKey" parameterType="java.lang.String">
    DELETE FROM bko_app_inherit
    WHERE id = #{id,jdbcType=VARCHAR}
  </delete>

  <delete id="deleteByAppId" parameterType="java.lang.String">
    DELETE FROM bko_app_inherit
    WHERE app_id = #{appId,jdbcType=VARCHAR}
  </delete>

  <delete id="deleteByParentId" parameterType="java.lang.String">
    DELETE FROM bko_app_inherit
    WHERE parent_id = #{parentId,jdbcType=VARCHAR}
  </delete>

  <select id="selectByPrimaryKey" parameterType="java.lang.String" resultMap="BaseResultMap">
    SELECT
    <include refid="Base_Column_List" />
    FROM bko_app_inherit
    WHERE id = #{id,jdbcType=VARCHAR}
  </select>

  <select id="selectByAppId" parameterType="java.lang.String" resultMap="BaseResultMap">
    SELECT
    <include refid="Base_Column_List" />
    FROM bko_app_inherit
    WHERE app_id = #{appId,jdbcType=VARCHAR}
  </select>

  <select id="selectByParentId" parameterType="java.lang.String" resultMap="BaseResultMap">
    SELECT
    <include refid="Base_Column_List" />
    FROM bko_app_inherit
    WHERE parent_id = #{parentId,jdbcType=VARCHAR}
  </select>
</mapper>