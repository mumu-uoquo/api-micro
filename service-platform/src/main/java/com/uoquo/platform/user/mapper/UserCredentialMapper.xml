<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.uoquo.platform.user.mapper.UserCredentialMapper">

  <resultMap id="BaseResultMap" type="com.uoquo.platform.user.model.pojo.UserCredential">
    <id column="id" jdbcType="VARCHAR" property="id" />
    <result column="user_id" jdbcType="VARCHAR" property="userId" />
    <result column="credential_type" jdbcType="VARCHAR" property="credentialType" />
    <result column="credential_value" jdbcType="VARCHAR" property="credentialValue" />
    <result column="institute_id" jdbcType="VARCHAR" property="instituteId" />
    <result column="create_time" jdbcType="TIMESTAMP" property="createTime" />
    <result column="update_time" jdbcType="TIMESTAMP" property="updateTime" />
    <result column="delete_state" jdbcType="INTEGER" property="deleteState" />
  </resultMap>

  <sql id="Base_Column_List">
    id, user_id, credential_type, credential_value, institute_id, create_time, update_time, delete_state
  </sql>

  <!-- 按凭证类型+值+机构查询；instituteId=null 时使用 IS NULL 判断 -->
  <select id="selectByCredentialType" resultMap="BaseResultMap">
    SELECT
    <include refid="Base_Column_List" />
    FROM bko_user_credential
    WHERE delete_state = 0
      AND credential_type  = #{credentialType,jdbcType=VARCHAR}
      AND credential_value = #{credentialValue,jdbcType=VARCHAR}
      <choose>
        <when test="instituteId == null">
          AND institute_id IS NULL
        </when>
        <otherwise>
          AND institute_id = #{instituteId,jdbcType=VARCHAR}
        </otherwise>
      </choose>
  </select>

  <!-- upsert 使用 MySQL ON DUPLICATE KEY UPDATE -->
  <insert id="upsertCredential">
    INSERT INTO bko_user_credential
        (id, user_id, credential_type, credential_value, institute_id,
         create_time, update_time, delete_state)
    VALUES
        (#{id,jdbcType=VARCHAR}, #{userId,jdbcType=VARCHAR}, #{credentialType,jdbcType=VARCHAR},
         #{credentialValue,jdbcType=VARCHAR}, #{instituteId,jdbcType=VARCHAR}, NOW(), NOW(), 0)
    ON DUPLICATE KEY UPDATE
        user_id          = VALUES(user_id),
        credential_value = VALUES(credential_value),
        update_time      = NOW(),
        delete_state     = 0
  </insert>

</mapper>
