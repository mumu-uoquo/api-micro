<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.uoquo.platform.user.mapper.UserSettingMapper">

    <!-- 基础结果映射 -->
    <resultMap id="BaseResultMap" type="com.uoquo.platform.user.model.pojo.UserSetting">
        <id column="id" jdbcType="VARCHAR" property="id"/>
        <result column="user_id" jdbcType="VARCHAR" property="userId"/>
        <result column="config_name" jdbcType="VARCHAR" property="configName"/>
        <result column="config_code" jdbcType="VARCHAR" property="configCode"/>
        <result column="config_value" jdbcType="VARCHAR" property="configValue"/>
        <result column="description" jdbcType="VARCHAR" property="description"/>
        <result column="update_user" jdbcType="VARCHAR" property="updateUser"/>
        <result column="update_time" jdbcType="TIMESTAMP" property="updateTime"/>
    </resultMap>

    <!-- 基础列名 -->
    <sql id="Base_Column_List">
        id, user_id, config_name, config_code, config_value, description, update_user, update_time
    </sql>

    <!-- 插入用户配置 -->
    <insert id="insert" parameterType="com.uoquo.platform.user.model.pojo.UserSetting">
        INSERT INTO bko_user_setting (id, user_id, config_name, config_code, config_value, description, update_user, update_time)
        VALUES (#{id,jdbcType=VARCHAR}, #{userId,jdbcType=VARCHAR}, #{configName,jdbcType=VARCHAR}, 
                #{configCode,jdbcType=VARCHAR}, #{configValue,jdbcType=VARCHAR}, #{description,jdbcType=VARCHAR}, 
                #{updateUser,jdbcType=VARCHAR}, #{updateTime,jdbcType=TIMESTAMP})
    </insert>

    <!-- 根据用户ID和配置标识更新 -->
    <update id="updateByUserIdAndCode" parameterType="com.uoquo.platform.user.model.pojo.UserSetting">
        UPDATE bko_user_setting
        SET config_name = #{configName,jdbcType=VARCHAR},
            config_value = #{configValue,jdbcType=VARCHAR},
            description = #{description,jdbcType=VARCHAR},
            update_user = #{updateUser,jdbcType=VARCHAR},
            update_time = #{updateTime,jdbcType=TIMESTAMP}
        WHERE user_id = #{userId,jdbcType=VARCHAR} AND config_code = #{configCode,jdbcType=VARCHAR}
    </update>

    <!-- 根据用户ID和配置标识删除 -->
    <delete id="deleteByUserIdAndCode">
        DELETE FROM bko_user_setting
        WHERE user_id = #{userId,jdbcType=VARCHAR} AND config_code = #{configCode,jdbcType=VARCHAR}
    </delete>

    <!-- 根据用户ID和配置标识查询 -->
    <select id="selectByUserIdAndCode" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM bko_user_setting
        WHERE user_id = #{userId,jdbcType=VARCHAR} AND config_code = #{configCode,jdbcType=VARCHAR}
    </select>

    <!-- 根据用户ID和前缀查询列表（使用CONCAT防止SQL注入） -->
    <select id="selectByUserIdAndPrefix" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM bko_user_setting
        WHERE user_id = #{userId,jdbcType=VARCHAR} AND config_code LIKE CONCAT(#{prefix,jdbcType=VARCHAR}, '%')
        ORDER BY config_code
    </select>

</mapper>